# Diagram PNGs — non-deterministic mmdc output, regenerate via ./typesetting/build.sh
typesetting/diagram/*.png

# Note: output/*.pdf is intentionally tracked — distributable artifacts for
# students/dosen who don't need (or want) the typst toolchain.

# Typst cache
.typst-cache/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
